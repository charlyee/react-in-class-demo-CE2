import * as React from 'react';

export interface INewerComponentProps {
}

export default class NewerComponent extends React.Component<INewerComponentProps> {
    public render() {
        return (
            <div>
                newer component example
      </div>
        );
    }
}
